namespace Reforge.Api.Common.Models;

/// <summary>Documentation-only shape for OpenAPI: matches GET /health's anonymous response
/// object. Never constructed at runtime — referenced only for Swagger schema.</summary>
public record HealthResponse(string Status);
